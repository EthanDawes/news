<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ethan's College Shenanigans: Home</title>
    <meta name="description" content="description"/>
    <meta name="author" content="Ethan Dawes" />
    <meta name="keywords" content="blog,newsletter" />
    <style>
      body {
        font-family: sans-serif;
      }
      ul {
        list-style-type: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: center;
        gap: 1rem;
      }
      li {
        border-radius: 1rem 1rem 0.5rem 0.5rem;
        overflow: hidden;
        max-width: 320px;
        border: 1px solid var(--color-text-main);
        background: var(--color-primary);
        min-width: 220px;
        height: 100%;
        transition: box-shadow 200ms;
        text-align: center;
      }
      li > img {
        width: 100%;
        height: 180px;
        object-fit: cover;
        display: block;
      }
    </style>
  </head>
  <body>
    <ul>
      {%- for post in collections.posts reversed -%}
        <a href="{{ post.url }}">
          <li>
            <img src="{% unless post.data.image contains '://' %}{{ post.url }}/{% endunless %}{{ post.data.image }}" alt="Post Image">
            {{ post.date | date: "%Y %b" }}</a>
          </li>
        </a>
      {%- endfor -%}
    </ul>
  </body>
</html>